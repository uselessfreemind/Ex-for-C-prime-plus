#include <stdio.h>
#include <string.h>
#define MAX 500
int main(void)
{
  char * out[MAX];

  char * input_sentence="";
  char * space=" ";
  unsigned int i;
  gets(input_sentence);
  if (strcmp(input_sentence++,space)
      i++;

    return 0;
}
